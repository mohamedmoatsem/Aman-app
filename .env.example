# Copy this file to .env and fill in your values.
# Never commit the real .env file to version control.

# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/aman_db

# Optional: override the Socket.IO server URL for the frontend
# Defaults to the same origin as the frontend if not set
VITE_SOCKET_URL=
